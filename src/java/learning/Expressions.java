package com.yusuf.learning;

public class Expressions {

    public static void main(String[] args) {
        // expression =     operands & operators
        // operands =  values, variables, numbers, quantity
        // operators = + - * / %

        int books = 20;
       // double books = 20;
       //use this if you are going to do this.
       // books = (double) books / 3;
       // This will give you the decimal
       //output is 6.666666666666667

        books = books + 1;
       // books++;
       // adds one ^
       // books = books - 5;
       // books--;
       // takes away one ^
       // books = books * 20;
       // books = books % 5;
       //gives you the remainder. 20/5 = 4 so there will be no remainder. but 20/6 will give you remainder
        System.out.println(books);
    }
}
